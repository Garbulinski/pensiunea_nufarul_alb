---
import ContactForm from '../../components/forms/ContactForm.astro';
import Layout from '../../layouts/Page.astro';
import { Container } from '@components/odyssey-theme';

const seo = {
	title: 'Contact | Astro Odyssey Theme',
	description: 'Contact Page',
};
---

<Layout seo={seo}>
	<Container>
		<section class="contact-hero__section">
			<div class="contact-hero__text-section">
				<h1>Contactati-ne</h1>
				<p>
					Doriti sa inchiriati o camera sau aveti o întrebare, un comentariu sau o cerere specială? Ne-am bucura sa primim vesti de la tine! 
					Ne puteti contacta la numarul afisat mai jos.
					Feedbackul dvs. este important pentru noi și suntem aici pentru a vă ajuta cu orice aveți nevoie.
					Vă mulțumim că ne-ați contactat și așteptăm cu nerăbdare să ne conectăm cu dvs. în curând!
				</p>
				<div>
					<p>
						<em><a href="tel:8008675309">0742 327 534</a></em> • <em
							><a href="mailto:hello@odysseytheme.com">nuraful_alb@gmail.com</a
							></em
						>
					</p>
				</div>
			</div>
			<div class="col-2__container">
				<ContactForm />
			</div>
		</section>
	</Container>
</Layout>

<style>
	.contact-hero__section {
		margin: var(--section-margin) auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		min-height: calc(90vh - var(--navbar-height));
		gap: 3rem;
	}
	.contact-hero__text-section {
		padding: 2rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
		background-color: var(--theme-surface-1);
		border-radius: var(--theme-shape-radius);
	}
	.col-2__container {
		display: flex;
		flex-direction: column;
		justify-content: center;
	}
	@media (max-width: 768px) {
		.contact-hero__section {
			grid-template-columns: 1fr;
		}
	}
</style>
